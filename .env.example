# Google OAuth Credentials
GOOGLE_CLIENT_ID="YOUR_CLIENT_ID_HERE"
GOOGLE_CLIENT_SECRET="YOUR_CLIENT_SECRET_HERE"
GOOGLE_PROJECT_ID="YOUR_PROJECT_ID_HERE"

# The redirect URI must exactly match one of the authorized redirect URIs
# for the OAuth 2.0 client in the Google Cloud Console.
# Default: http://localhost:3000/callback/oauth2callback
OAUTH_REDIRECT_URI="http://localhost:3000/callback/oauth2callback"

# Flask settings
PORT=3000
FLASK_SECRET_KEY="YOUR_SUPER_SECRET_KEY_HERE"

# Generate a secure Flask secret key with:
# python -c 'import os; print(os.urandom(24).hex())'
